interface CardNoteProps {
  id: string;
  title: string;
  content: string;
  date: string;
}

const CardNote = () => {
  return (
    <div>
      Card Note
    </div>
  );
};

export default CardNote;
